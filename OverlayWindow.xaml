<Window x:Class="FlashscoreOverlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Flashscore Overlay" 
        Height="Auto" 
        Width="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        SizeToContent="Height"
        MaxHeight="800">
    
    <Border Background="#FF000000" 
            BorderBrush="#FF333333" 
            BorderThickness="1" 
            CornerRadius="8"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Direction="320" 
                            ShadowDepth="5" 
                            Opacity="0.8" 
                            BlurRadius="20"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="#FF1A1A1A" 
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="10,0">
                    <TextBlock Text="⚽ Partidos Seleccionados" 
                              Foreground="White" 
                              FontWeight="Bold" 
                              FontSize="12"
                              VerticalAlignment="Center"/>
                    
                    <Button Content="✕" 
                            Width="25" 
                            Height="25" 
                            HorizontalAlignment="Right"
                            Background="#FFC80037"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Click="Close_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFA0002B"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Contenido -->
            <ScrollViewer Grid.Row="1" 
                         VerticalScrollBarVisibility="Auto"
                         MaxHeight="700">
                <StackPanel x:Name="MatchesPanel" 
                           Margin="10"
                           Background="#FF000000"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
